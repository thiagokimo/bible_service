<h1>Listing books</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Link</th>
    <th></th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><%= book.name %></td>
    <td><%= book.link %></td>
    <td><%= link_to 'Show', book_chapters_path(book) %></td>
  </tr>
<% end %>
</table>

<br />
